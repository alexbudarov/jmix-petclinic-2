scrape_configs:
  - job_name: 'PetClinicMetrics'
    metrics_path: '/petclinic/actuator/prometheus'
    scrape_interval: 5s
    static_configs:
      - targets: ['172.17.0.1:8080'] # this IP address means "host machine" on Linux
        labels:
          application: 'Pet Clinic'
